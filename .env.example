# Copy to .env and fill in. Never commit .env.
# Anthropic API key - required for live extraction/theming and the live Q&A agent.
ANTHROPIC_API_KEY=
# Kaggle API token (Settings > API > Create token) - required only to re-download raw data.
KAGGLE_API_TOKEN=
